import math
def S(n):
    cube_full_nums = [1]
    for k in range(2,math.floor(n**(1 / 3)) + 1):
        print(k)
        cf = k**3
        while cf < n + 1:
            cube_full_nums.append(cf)
            cf *= k
    cube_full_nums = list(dict.fromkeys(cube_full_nums))
    sigma_s = 0
    for cfn in cube_full_nums:
        print(cfn)
        sigma_s += n // cfn
    return sigma_s
    
print(S(10**4))
